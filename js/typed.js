document.addEventListener("DOMContentLoaded", function () {
  var options = {
    strings: ["Mobile Money, Visa, or Credit Card."],
    typeSpeed: 50,
    backSpeed: 50,
    backDelay: 4000,
    startDelay: 100,
    showCursor: true,
    cursorChar: "",
    loop: true,
  };

  var typed = new Typed(".typed-text", options);
});
