document.addEventListener("DOMContentLoaded", function () {
  var options = {
    strings: ["Empowering Digital Democracy and Event Experiences"],
    typeSpeed: 50,
    backSpeed: 50,
    backDelay: 4000,
    startDelay: 100,
    showCursor: true,
    cursorChar: "",
    loop: false,
  };

  var typed = new Typed(".typed-text", options);
});
